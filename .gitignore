venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/

# Results ARE committed: a reviewer must be able to inspect every published
# number without re-running a GPU job. Only large intermediates are excluded.
results/**/*.pt
results/**/*.safetensors
